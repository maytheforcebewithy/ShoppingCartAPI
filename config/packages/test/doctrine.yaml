doctrine:
    dbal:
        driver: pdo_pgsql
        host: '%env.test(resolve:DATABASE_HOST)%'
        port: '%env.test(resolve:DATABASE_PORT)%'
        dbname: '%env.test(resolve:DATABASE_NAME)%'
        user: '%env.test(resolve:DATABASE_USER)%'
        password: '%env.test(resolve:DATABASE_PASSWORD)%'
        charset: utf8mb4
